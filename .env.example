# ============================================
# TechARA Project - Complete .env Configuration
# ============================================
# Copy this file to .env and fill in the values for your environment

# ============================================
# SERVER & RUNTIME CONFIGURATION
# ============================================

# Node environment mode
# Values: development | production
NODE_ENV=development

# Server port (Express will listen on this port)
# Default: 5000
PORT=5000

# Replit platform identifier (auto-set, no need to change)
# REPL_ID=your-repl-id

# ============================================
# FRONTEND CONFIGURATION (VITE_* PREFIX)
# ============================================

# API base URL - where frontend sends requests to
# IMPORTANT: In Replit preview, use relative paths (leave empty or use /)
# Leave empty to use same origin as current page
# For PRODUCTION/ONLINE with separate domain: https://yourdomain.com
# Examples:
#   VITE_API_URL=  (empty - uses current origin, best for Replit)
#   VITE_API_URL=/api  (relative path)
#   VITE_API_URL=https://yourdomain.com  (for external API)
VITE_API_URL=

# WhatsApp contact number for registration/contact forms
# Format: +CountryCodePhoneNumber (no spaces, no dashes)
# Used by RegistrationPopup component for WhatsApp integration
# Example: +919345791995 (India number example)
VITE_WHATSAPP_NUMBER=+919345791995

NODE_ENV=development

VITE_API_URL=

VITE_WHATSAPP_NUMBER=+919345791995

DATABASE_URL=postgresql://neondb_owner:npg_kY4EnONt5Mih@ep-red-bird-adf9u5ob-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# ============================================
# DATABASE CONFIGURATION
# ============================================

# Primary database URL (PostgreSQL/Neon)
# Used by Drizzle ORM for database operations
# Format: postgresql://username:password@host:port/database_name
# Get this from:
#   - Replit Database panel (if using Replit's built-in DB)
#   - Neon dashboard (if using Neon PostgreSQL)
#   - Your PostgreSQL provider
DATABASE_URL=postgresql://user:password@localhost:5432/techara

# Neon-specific database URL (optional, if using Neon)
# Can be the same as DATABASE_URL or different
NEON_DATABASE_URL=postgresql://neon_user:neon_password@ep-xyz.neon.tech/database_name

# PostgreSQL connection details (optional, for reference)
# Replit Database automatically sets these
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=techara

# ============================================
# THIRD-PARTY INTEGRATIONS
# ============================================

# N8N Webhook for automated email/notification workflows
# Used in: client/src/components/RegistrationPopup.tsx
# This webhook receives registration data and sends emails
N8N_WEBHOOK_URL=https://brotechapp.app.n8n.cloud/webhook/TechAra

# ============================================
# RAZORPAY (SERVER ONLY)
# ============================================
# DO NOT put secrets in client/.env
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
RAZORPAY_WEBHOOK_SECRET=

# ============================================
# NOTES
# ============================================
# - Do NOT commit the actual .env file to git (it contains secrets)
# - Use .env.example for documentation (no secrets)
# - In Replit, use the "Secrets" panel instead of .env file
# - Prefix VITE_ for variables accessible in frontend (React)
# - Prefix with nothing for server-only variables (Node.js)
